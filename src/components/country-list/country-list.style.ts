import styled from "styled-components";

export const ListContainer = styled.div`
  display: block;
  position: fixed;
  height: 80vh;
  width: auto;
  padding: 5px;
  margin: 0 auto;
  overflow-y: scroll;
  transform: translateX(-5px);
`;
