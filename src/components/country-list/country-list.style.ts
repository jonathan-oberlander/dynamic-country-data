import styled from "styled-components";

export const ListContainer = styled.div`
  display: block;
  position: fixed;
  height: 90vh;
  width: auto;
  padding: 15px;
  margin: 0 auto;
  overflow-y: scroll;
  transform: translateX(-15px);
`;
